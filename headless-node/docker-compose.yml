services:
  headless-node:
    build:
      context: ..
      dockerfile: headless-node/Dockerfile
    environment:
      - VITE_WEBSOCKET_URL=ws://host.docker.internal:9003/ws
      - NODE_COUNT=1
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
